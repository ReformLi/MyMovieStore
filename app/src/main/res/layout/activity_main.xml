<?xml version="1.0" encoding="utf-8"?>
<!--
  主页面容器：顶部导航（首页 / 搜索 / 我的） + ViewPager2 内容区。

  导航栏有两套实现，运行时二选一：
   - 手机端：BottomNavigationView（id 沿用历史命名 bottomNavigation，铺满整行）
   - 电视端：tvNavBar（横向 LinearLayout，紧凑页签簇，底部橙色指示条，适配 10-foot UI）
  两者都置于 navContainer 内。
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/navContainer"
        android:layout_width="0dp"
        android:layout_height="44dp"
        android:background="@color/colorSurface"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- 手机端导航：铺满整行 -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            app:elevation="0dp"
            app:itemIconSize="20dp"
            app:itemPaddingTop="0dp"
            app:itemPaddingBottom="2dp"
            app:itemIconTint="@color/bottom_nav_color"
            app:itemTextColor="@color/bottom_nav_color"
            app:itemTextAppearanceActive="@style/BottomNavText"
            app:itemTextAppearanceInactive="@style/BottomNavText"
            app:menu="@menu/bottom_nav_menu" />

        <!-- 电视端导航：居中页签簇，默认隐藏，仅 TV 显示 -->
        <LinearLayout
            android:id="@+id/tvNavBar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_horizontal"
            android:visibility="gone" />

    </FrameLayout>

    <!-- 导航栏与内容区之间的细分隔线 -->
    <View
        android:id="@+id/navDivider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/navContainer" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/navDivider" />

</androidx.constraintlayout.widget.ConstraintLayout>
