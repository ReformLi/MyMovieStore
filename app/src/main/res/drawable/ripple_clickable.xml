<?xml version="1.0" encoding="utf-8"?>
<!-- 只有按压涟漪、**没有焦点态**的可点击背景（TV 端专用替代品）。

     为什么不能再用 `?attr/selectableItemBackground`：
     在 AppCompat 主题下它解析成 **Holo 遗留选择器** abc_item_background_holo_dark/light，
     自带焦点态（#33B5E5 @60% 蓝）与按压态（#40CCCCCC 25% 白）高亮。控件的系统默认焦点高亮
     已被 TvFocus 关掉、改由自绘橙环负责，这一层外来高亮会盖在控件底色之上与橙环抢戏 ——
     用户看到的就是「一聚焦/一按下，底色自己变蓝变白」。

     ripple 颜色刻意不用 ?attr/colorControlHighlight：本 App 把它设成了不透明的
     @color/colorSurfaceHighlight(#F0F0F0)，当涟漪色会刷出一整块实色。这里用 20% 白，
     深色底（TV 默认）与浅色底都可见。

     mask 层只做波纹遮罩、不参与绘制。 -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#33FFFFFF">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="10dp" />
        </shape>
    </item>
</ripple>
