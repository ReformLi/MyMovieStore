<?xml version="1.0" encoding="utf-8"?>
<!-- 勾选圆：未选中态（透明底 + 分割线色描边圆环） -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2dp"
        android:color="@color/colorDivider" />
</shape>
